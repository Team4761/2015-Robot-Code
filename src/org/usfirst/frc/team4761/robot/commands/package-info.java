/**
 * Commands that do exactly one thing each. This package is only for stuff that
 * doesn't fit into the other subpackages. An example of this might be a
 * command which requires multiple subsystems (though if this is the case you
 * should probably be looking at using a command group instead).
 */
package org.usfirst.frc.team4761.robot.commands;